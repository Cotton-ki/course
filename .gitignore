#include<bits/stdc++.h>
using namespace std;
int main(){
   ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
   int n;
   cin>>n;
   vector<int> a;
   for (int i = 1; i <=n; i++)
      a.push_back(i);
   do{
      for(auto i:a)
         cout <<i<<" ";
      cout<<"\n";

   }while (next_permutation(a.begin(), a.end()));
   return 0;
}
